#!/usr/bin/python3
def square_matrix_simple(matrix=[]):
    square = []
    for line in matrix:
        square.append([c**2 for c in line])
    return squared

after creating each file, cat 0-main.py and copy the code they gave as an 0-mmain

